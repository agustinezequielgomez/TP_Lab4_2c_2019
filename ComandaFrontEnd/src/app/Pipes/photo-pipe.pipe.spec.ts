import { PhotoPipePipe } from './photo-pipe.pipe';

describe('PhotoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PhotoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
