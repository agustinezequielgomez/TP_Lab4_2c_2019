import { DisableOrdersDirective } from './disable-orders.directive';

describe('DisableOrdersDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOrdersDirective();
    expect(directive).toBeTruthy();
  });
});
