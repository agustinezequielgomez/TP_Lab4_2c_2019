import { RegisterEmployeeRequest } from './register-employee-request';

describe('RegisterEmployeeRequest', () => {
  it('should create an instance', () => {
    expect(new RegisterEmployeeRequest()).toBeTruthy();
  });
});
