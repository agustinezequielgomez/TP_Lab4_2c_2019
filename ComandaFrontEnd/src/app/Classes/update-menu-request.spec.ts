import { UpdateMenuRequest } from './update-menu-request';

describe('UpdateMenuRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateMenuRequest()).toBeTruthy();
  });
});
