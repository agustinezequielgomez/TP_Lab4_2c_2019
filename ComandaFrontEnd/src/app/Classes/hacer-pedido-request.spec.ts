import { HacerPedidoRequest } from './hacer-pedido-request';

describe('HacerPedidoRequest', () => {
  it('should create an instance', () => {
    expect(new HacerPedidoRequest()).toBeTruthy();
  });
});
