import { GenerateOrderResponse } from './generate-order-response';

describe('GenerateOrderResponse', () => {
  it('should create an instance', () => {
    expect(new GenerateOrderResponse()).toBeTruthy();
  });
});
